<%= form_for(@qmail) do |f| %>
  <% if @qmail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qmail.errors.count, "error") %> prohibited this qmail from being saved:</h2>

      <ul>
      <% @qmail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
